# Java 17 安装说明

## ✅ 已创建的安装脚本

我已经为您创建了自动安装脚本：`install-java17.sh`

## 🚀 快速安装（推荐）

### 方法一：使用安装脚本（最简单）

在终端中运行以下命令：

```bash
cd /Users/wangyilin/workSpace/uni-preset-vue-vite
bash GeneAI-Java/install-java17.sh
```

**注意**：安装过程中会提示您输入管理员密码，请按提示操作。

### 方法二：手动逐步安装

如果脚本无法运行，可以手动执行以下步骤：

#### 1. 安装 Homebrew

```bash
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
```

根据提示输入密码，然后按照终端提示执行：

**Apple Silicon (M1/M2/M3) Mac:**
```bash
echo 'eval "$(/opt/homebrew/bin/brew shellenv)"' >> ~/.zshrc
eval "$(/opt/homebrew/bin/brew shellenv)"
```

**Intel Mac:**
```bash
echo 'eval "$(/usr/local/bin/brew shellenv)"' >> ~/.zshrc
eval "$(/usr/local/bin/brew shellenv)"
```

#### 2. 安装 Java 17

```bash
brew install openjdk@17
```

#### 3. 配置环境变量

```bash
echo '' >> ~/.zshrc
echo '# Java 17 Configuration' >> ~/.zshrc
echo 'export JAVA_HOME=$(/usr/libexec/java_home -v 17 2>/dev/null || echo "")' >> ~/.zshrc
echo 'if [ -n "$JAVA_HOME" ]; then' >> ~/.zshrc
echo '    export PATH=$JAVA_HOME/bin:$PATH' >> ~/.zshrc
echo 'fi' >> ~/.zshrc
source ~/.zshrc
```

#### 4. 验证安装

```bash
java -version
# 应该显示：openjdk version "17.x.x" 或类似内容
```

## 📦 方法三：直接下载安装（不使用 Homebrew）

如果不想使用 Homebrew，可以从以下网站直接下载安装：

### 推荐：Azul Zulu OpenJDK（简单易用）

1. 访问：https://www.azul.com/downloads/?package=jdk#zulu
2. 选择 macOS ARM64（M1/M2/M3）或 macOS x64（Intel）
3. 选择 Java 17 LTS
4. 下载 `.dmg` 文件并安装

### 或使用 Oracle JDK

1. 访问：https://www.oracle.com/java/technologies/downloads/#java17
2. 选择 macOS 版本
3. 下载并安装

### 安装后配置环境变量

编辑 `~/.zshrc` 文件，添加：

```bash
export JAVA_HOME=$(/usr/libexec/java_home -v 17)
export PATH=$JAVA_HOME/bin:$PATH
```

然后运行：
```bash
source ~/.zshrc
java -version
```

## ✅ 安装完成后

安装完成后，运行以下命令验证：

```bash
java -version
javac -version
/usr/libexec/java_home -V
```

如果看到 Java 17，说明安装成功！

## 🔧 配置 IDE

安装完成后，请重启 Cursor/VS Code，IDE 应该能够识别 Java 17 了。

如果还有问题，请检查：
1. `.vscode/settings.json` 文件（已创建）
2. `.settings/org.eclipse.jdt.core.prefs` 文件（已创建）

## ❓ 需要帮助？

如果遇到问题，请提供：
- `java -version` 的输出
- 错误信息
- 您使用的 Mac 型号（Intel 还是 Apple Silicon）



# Java后端环境配置指南

## 📋 当前状态检查

### 1. 检查Java版本

```bash
java -version
```

**要求**：Java 17 或更高版本

**当前状态**：您的系统显示 Java 1.8，需要升级到 Java 17+

### 2. 检查Maven

```bash
mvn -version
```

**要求**：Maven 3.6+ 

**当前状态**：Maven未安装

## 🔧 安装步骤

### 方式一：使用Homebrew安装（推荐，macOS）

#### 1. 安装Homebrew（如果未安装）

```bash
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
```

#### 2. 安装Java 17

```bash
brew install openjdk@17
```

#### 3. 配置Java环境变量

```bash
# 添加到 ~/.zshrc 或 ~/.bash_profile
export JAVA_HOME=$(/usr/libexec/java_home -v 17)
export PATH=$JAVA_HOME/bin:$PATH
```

#### 4. 重新加载配置

```bash
source ~/.zshrc  # 或 source ~/.bash_profile
```

#### 5. 验证Java版本

```bash
java -version
# 应该显示 java version "17.x.x"
```

#### 6. 安装Maven

```bash
brew install maven
```

#### 7. 验证Maven

```bash
mvn -version
```

### 方式二：手动安装（不使用Homebrew）

#### 1. 下载并安装Java 17

访问：https://www.oracle.com/java/technologies/downloads/#java17

或使用OpenJDK：https://adoptium.net/

#### 2. 下载并安装Maven

访问：https://maven.apache.org/download.cgi

下载 `apache-maven-3.x.x-bin.tar.gz`

```bash
# 解压
tar -xzf apache-maven-3.x.x-bin.tar.gz
sudo mv apache-maven-3.x.x /usr/local/maven

# 配置环境变量（添加到 ~/.zshrc）
export M2_HOME=/usr/local/maven
export PATH=$M2_HOME/bin:$PATH
```

## ✅ 验证安装

### 检查Java

```bash
java -version
# 应该显示：java version "17.x.x" 或更高
```

### 检查Maven

```bash
mvn -version
# 应该显示Maven版本信息
```

### 检查Java编译器

```bash
javac -version
# 应该显示：javac 17.x.x 或更高
```

## 🚀 启动后端

安装完成后，启动后端：

```bash
cd GeneAI-Java

# 方式1：使用Maven直接运行
mvn spring-boot:run

# 方式2：先编译再运行
mvn clean package
java -jar target/geneai-java-1.0.0.jar
```

## 🔑 配置API Key

启动前，确保配置了OpenAI API Key：

```bash
export OPENAI_API_KEY=your_api_key_here
```

或编辑 `src/main/resources/application.yml`

## 📝 常见问题

### 问题1：Java版本不匹配

**错误**：`UnsupportedClassVersionError`

**解决**：确保使用Java 17+，检查 `java -version`

### 问题2：Maven命令未找到

**错误**：`command not found: mvn`

**解决**：
1. 确保Maven已安装
2. 检查PATH环境变量
3. 重新加载shell配置：`source ~/.zshrc`

### 问题3：端口被占用

**错误**：`Port 3000 is already in use`

**解决**：
```bash
# 查找占用端口的进程
lsof -ti:3000

# 停止进程
kill -9 $(lsof -ti:3000)
```

## 🆘 需要帮助？

如果遇到问题，请提供：
1. `java -version` 的输出
2. `mvn -version` 的输出（如果已安装）
3. 错误信息的完整内容

